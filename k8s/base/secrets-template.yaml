# ⚠️  TEMPLATE ONLY — fill values then: kubectl apply -f secrets-template.yaml
# Never commit real passwords to git. Use this only to create the secret manually.
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: trading-platform
type: Opaque
stringData:
  DB_USER:      "trading"
  DB_PASSWORD:  "CHANGE_ME_STRONG_PASSWORD_HERE"
  DATABASE_URL: "postgresql://trading:CHANGE_ME_STRONG_PASSWORD_HERE@postgresql:5432/tradingdb"
  JWT_SECRET:   "CHANGE_ME_JWT_SECRET_MINIMUM_32_CHARS_LONG_HERE"
